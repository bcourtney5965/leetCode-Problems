// i: input - two objects (two binary trees)
// o: ouput - boolean
// c: constraints

// * notes to Zack or myself

// Instructions
// Given two binary trees, write a function to check if they are equal or not.

// Two binary trees are considered equal if they are structurally identical and 
// the nodes have the same value.

/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
/**
 * @param {TreeNode} p
 * @param {TreeNode} q
 * @return {boolean}
 */
// var isSameTree = function(p, q) {
    
// };



// PSEUDO CODE
// initialize isSameTree function with two params- two objects (p, q)
  // if p.val doens't equal q.val
    // return false
  // if p.val and q.val both equal null
    // return true
  // if p.left and q.left don't equal null
    // return isSameTree(p.left, q.left)
  // if p.right and q.right don't equal null
    // return isSameTree(p.right, q.right)  
// close function 

  




// CODE
